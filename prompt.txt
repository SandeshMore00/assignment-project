below prompt is use for create frontend 
Note: backend code is not generated by prompt


I want to build a simple React frontend component for searching companies using an API. 
create a functional React component called Search using hooks. The component should:
Have an input field where the user can type a search query
Have a Search button to trigger the API call
Call a GET API endpoint like: /api/search?query=<user_input>
Show a loading state while the request is in progress
Display a message like "No results found" if the API returns an empty array
Display the results in a list format (each item showing company name)
Handle errors gracefully and avoid breaking the UI

State management requirements:

Store the input query
Store the API results
Track loading state
Track whether a search has been performed (to conditionally show "No results found")

UI requirements:

Keep it simple (basic input + button + list)
Add some minimal inline spacing (no need for heavy styling)
Show "Loading..." while fetching data

Also:

Use fetch for API calls
Use useState for state management
Ensure empty queries are not sent to the API

Finally, render this component inside App.jsx.





